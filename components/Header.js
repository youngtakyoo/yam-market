import React from "react";

const Header = (props) => {

    return (
        <React.Fragment>
            헤더 입니다.
        </React.Fragment>
    )
};

Header.defaultPorps = {
    
}

export default Header;